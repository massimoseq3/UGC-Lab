# CLAUDE.md — UGC OS

## Project Identity

You are building **UGC OS** — a browser-based, macOS-style workspace that unifies eight tools for UGC ad production into one environment with shared data banks. Every in-app AI call goes through **kie.ai**: one user-supplied Bearer key gives access to every model the app uses (chat, vision, image, video, TTS, music). The one exception is **Edit**, which generates nothing in the browser — it hands out a Claude skill the member runs locally.

**Who it's for / why.** A private Skool community of solo creators and small teams producing AI UGC ads at scale — access is gated by an email allowlist synced from Skool, so non-members can't sign up even with the URL. The pitch is one workspace instead of seven tabs: the same product, character, script, voice, and B-Roll flow across every app via the shared banks. Inference cost is on the member (BYO kie.ai key), so the operator only pays for hosting + storage.

Dock order: **Dashboard** (own 'system' group) | **Bank** | **the Create apps** (Characters → Scripts → Voiceovers → B-Roll → **Edit**, which closes the row — cut what you produced) | **Tools** (Ad Analyzer, Playground) | Settings utility tile. Fresh visits land on the Dashboard (`DEFAULT_SLUG`). Display names are terse nouns, not action verbs. Folder names and the `id` strings in `src/utils/constants.ts` are **stable** — they key per-app model selections in localStorage. Do not rename them.

| Section | Sidebar entry | Folder | Job |
|---|---|---|---|
| Library | Bank | `finder/` | Banks browser (Products / Characters / Scripts / Voices / B-Rolls / **Styles**). Styles holds the saved visual looks B-Roll distils from reference frames — `StyleForm` edits the name + brief (the frames are read-only thumbnails). Product form: drop a photo (and optionally paste listing/product-page copy — authoritative for claims/specs/offer) to auto-fill via `extractProductInfo` (`CHAT_MODEL_DEFAULT`), re-runnable from the "Auto-fill" button; products carry optional deep-context fields `keySpecs` / `customerLanguage` / `objections` that flow into Scripts + B-Roll prompts. A product also carries up to four **extra angles** (`Product.extraImages`, "More Angles" strip under the hero shot) — the box open, what's inside, the label. They ride along in the same auto-fill vision call (one object, several shots — the prompt says so explicitly, so it never reads them as a bundle), and every **image-picking** BankPicker (`expandProductImages`: RefTiles / FrameSlot / VideoInputSlot / B-Roll's `ExtraRefsRow` / the Influencers edit modal) lists one tile per photo so a scene that needs the open box can attach the open box. Pickers that pick the *product itself* (B-Roll's product slot, Scripts) leave the flag off — they need the real row id. |
| Create | Influencers | `character-studio/` | Form → portrait image, or (Portrait / Influencer Sheet toggle) a multi-panel reference sheet (turnaround + expressions + full body; scene/pose/camera fields ignored; prompt layout swaps with orientation). **The Portrait / Sheet toggle picks the output kind ONLY** — resolution and aspect are shared across both modes and never reset by a flip (both used to, silently undoing a deliberate 4K or 9:16 pick and multiplying the credit cost); a sheet's grid needs a long axis, so 1:1 renders it vertically without touching the stored portrait aspect. Same rule in `InfluencerEditModal`'s Edit Character / Character Sheet toggle, which seeds both settings from the image being edited. Resolution defaults to 1K (key `:resolution:v3`). Sheets save as their own bank Model entry (named like a portrait; stamped as both `characterImage` and `sheetImage`). Horizontal (16:9) outputs span a full row in the gallery, and in `BankPicker` a sheet spans both columns only when its image measures landscape (`onLandscape`) — a 9:16 sheet is a normal one-column tile. Drop a reference photo anywhere to auto-fill every field via vision-based DNA extraction. Display name rebranded from "Characters" — folder, ids, types, and LLM prompts keep the character naming. |
| Create | Scripts | `script-architect/` | Two modes: **Remix** (one source box; format auto-detected via `detectSceneBlueprint` — a winning transcript → 3 variations, an Ad Analyzer scene blueprint (`--- Scene N` / `SCENE N —` headers) → rewritten scene prompts, with a chip to override back to plain remix) and **Write New** (brief + style + 10/15/30/60s → 3 human-sounding takes, as plain script or scene blueprint). The three LLM pipelines and `ScriptMode` ('write' / 'remix' / 'reverse-engineer') survive unchanged in the service and history rows — only the UI toggle merged. Write New's **Output** toggle: Script / Hooks / Scenes / Cinematic. **Script Style is two groups in one picker** (`WriteStyleGroup`, sectioned in the slide-over): **Structures** — the persuasion mechanic (PAS, story, 3 Reasons, callout, curiosity, before/after, demo, us-vs-them, objection-crusher, founder) — and **Formats** — the kind of organic content the ad imitates (podcast clip, street interview, green-screen reaction, comment reply, expert explainer, how-to, GRWM), which is the higher-converting half because the ad reads as content before it reads as an ad. Only formats carry `WRITE_STYLE_SCENE_DIRECTION`, appended in the **Scenes** output so the blueprint actually stages the format (boom mic + studio angles, a handheld mic entering frame off a street, an on-screen review card) instead of defaulting to selfie-to-camera; a structure implies no camera, so its shots stay free. When a format's way of opening collides with the take's opening device, `FORMAT_OVERRIDES_TAKE` gives the format the delivery and the take keeps its angle. Style slugs are **stable** — they key persisted picks and history rows. **Hooks** (`writeFormat: 'hooks'`) writes 10 standalone opening lines from the "1,000 Viral Hooks" swipe file distilled into 7 formula families (`HOOK_CATEGORY_META`: Educational / Comparison / Myth Busting / Storytelling / Authority / Day in the Life / Pattern Interrupt); the Hook Style picker defaults to **Best Mix** (LLM picks ≥4 fitting families) or locks one family; no length/style controls; a single LLM call returns `<FAMILY>`-tagged lines (`parseHooks`) rendered as per-hook rows with a family chip + copy — copy/save strip the tags, and there are no send buttons. Formulas are kept at full original length (setup + payoff, never truncated), and the same `HOOK_LIBRARY` + `HOOK_OPENING_INSTRUCTION` seed the opening line of Write New's Script and Scenes outputs. **Cinematic** (`writeFormat: 'prompt'`): generates 3 distinct ≤15s single-clip AI-commercial master prompts (generic STYLE→TIMELINE formula, `@INFLUENCER`/`@PRODUCT` tokens) — swaps the Script Style picker for an optional Influencer (models bank) picker, caps length to 10s/15s, and ships each via **Send to Playground** (`cinematicVideo` payload → video mode, refs attached, Seedance 2.0). **Scene tokens:** `[PRODUCT]` / `[CHARACTER]` are reference-image slots and belong in **visual direction only** — never inside a spoken line, which TTS and video models pronounce literally. Dialogue always names the product in plain words, at most twice (`nameSpokenTokensInDialogue` is the deterministic backstop when the model ignores that). **Batch size is user-picked — 3 / 5 / 10, default 3** (`VARIATION_COUNTS`, a `SegmentedToggle` above Generate; Hooks exempt and hidden, `HOOK_COUNT` stays 10). Five parallel takes off one brief crowded each other, so 3 is the default, but the wide net stays one click away. Every angle list (`WRITE_TAKE_INSTRUCTION`, `WRITE_PROMPT_TAKE_INSTRUCTION`, `REMIX_ANGLES`) holds **10 entries ordered strongest-first** and is **sliced** to the chosen count — so picking 3 gets the three best angles, and a count can never index past its pool. `RemixAngle` has 10 members; `hook-led` sits LAST because as an angle it mostly restates the global hook rules. A remix **stamps the angles it used** onto `GeneratedScript` + the history row (`remixAngles`, `variationCount`) and `OutputPanel` prefers that stamp; rows saved before the picker carry no stamp and are matched by count via `remixAnglesForCount`, which keeps the pre-picker five-variation order distinct from today's first five (a different set). |
| Create | Voiceovers | `voice-studio/` | Script → audio (Gemini 3.1 Flash TTS). 30 named Google voices, picker **grouped by gender** (Female 14 / Male 16, split per Google's docs; each voice keeps a vibe descriptor tag). The picker generates a short sample **on demand** and caches it per session (`services/previewVoice.ts`) since Gemini has no hosted preview files. Right panel order: **Preset** → Voice → Style → Pace+Accent → Expressiveness → Scene → Tone/context → direction chips. The **Preset** row loads a saved `voices` bank entry through `BankPicker` (`settingsFromPreset` flattens it through `sanitizeVoiceSettings`, so a preset written against an older option list still loads) — voice, delivery params AND its scene/tone in one tap; presets carry `scene`/`sampleContext`, editable in the Bank's `VoiceForm`. The row's stamp (`VoiceSettings.presetId`/`presetLabel`, label-only, never sent to kie) is dropped by ANY hand edit — every control in `SettingsView` goes through one `update()` helper, plus the voice picker, "Reset values" and a history restore — so the row can't name a preset the settings no longer hold. `DIRECTION_PRESETS` are the one-tap chips under Scene/Tone: they fill both boxes, and tapping the active chip clears them. `style` / `pace` / `accent` are custom portaled `Dropdown`s (via `AnchoredPopover`) whose options are kie.ai's **exact** enums — Style (Vocal Smile/Newscaster/Whisper/Empathetic/Promo·Hype/Deadpan, default Vocal Smile not kie's Deadpan), Pace (Natural/Rapid Fire/The Drift/Staccato), Accent (Neutral/American Gen·Valley·South/British RP·Brixton/Transatlantic/Australian) — verified at kie.ai/gemini-3.1-flash-tts; don't invent values. `temperature` is the "Expressiveness" slider; `scene`/`sampleContext` optional. `voiceId` holds the Gemini `voice_name`. `sanitizeVoiceSettings` coerces persisted style/pace/accent that predate an option-list change. |
| Create | B-Roll | `broll-studio/` | (History `BrollHistoryView` — a **card grid**, day-sectioned under a centred `DayPill`, not a list of thin rows: a session's own footage is the thing worth recognising, and 40px round thumbs on a 70%-wide panel showed almost none of it. Each card is a media cover over a two-line title + meta: **mode filter pills** — All / Line-by-Line / Continuous, keyed off each row's derived mode, shown only when >1 mode is present — plus a persisted **sort dropdown** (`broll-studio:historySort`: Newest / Oldest by `createdAt`, Recently updated by `updatedAt`) that drives ordering + day-grouping + the row's relative-time label together. Cards carry a **mode badge** (top-left) + scene count and a **visual-style pill** (`historyStyleLabel` → `getContinuousStyle` label, else the saved style's `styleName`, else "Custom style"; the row-level `styleId`/`styleBrief`/`styleName` snapshot covers Continuous, whose result carries no brief) over the cover, and a finished-media tally (`mediaTally` → "6 stills · 2 clips") beside the timestamp below it. The **cover** is a mosaic of up to three pieces of the session's own media (`historyCovers` — 1 full-bleed / 2 split / 3 as big-plus-stacked-pair), drawn from either mode: stills first, then clip first-frame posters; a session with no media yet gets the film glyph. Delete sits in a `TileActionStack` on the cover (media variant), and an in-flight session gets the rect `GeneratingPulseRing` + its live count in place of the tally. Rows from the retired One-Shot mode are **filtered out of the list** by `isRetiredOneShotRow` (applied in `RightPanel` so the tab's count and the list agree) — the rows and their clips stay on disk and in the cloud, so the mode can come back without losing anyone's work. `BrollHistoryItem.createdAt` is **write-once** (`upsertBrollHistory` preserves it) and an upsert whose content is unchanged is a **no-op**, so merely reopening a row neither bumps it under "Recently updated" nor re-pushes to the cloud; the FIFO cap evicts by `createdAt`, not by array position. Search matches the row's displayed title (product · influencer · script, resolved once in the parent) as well as `inputSummary`.) Two modes via a left-panel header toggle (`:mode`, default `'line'`; order Line-by-Line → Continuous, hyphenated labels + icons, aligned with the right panel's Scenes/History strip; a persisted `'oneshot'` sanitizes to `'line'`). Both Generate buttons carry an estimated-credits pill (`services/promptCost.ts` — chat is per-1k-tokens, so it's a deliberately rough, rounded-up figure that lands under a credit or two; it exists so nothing fires unpriced). **Import prompts** (`services/importPrompts.ts` + `ImportPromptsModal`, a grey pill paired with the panel's **New** reset in the References eyebrow row — same size and style as `ClearAllButton`, present in both modes): paste — or drop a .txt/.md/.xml/.json file of — a storyboard written outside the app and skip the prompt-writing call entirely (the member's own Claude subscription instead of their kie credits). Two rules hold it together: it runs through **the same parsers** the live call uses (`parseScenes` / `parseContinuousResult`), and the modal's **Copy brief** hands the outside model **the app's own system + user prompt** for that mode (`brollSystemInstruction` + `buildBrollUserPrompt` / `CONTINUOUS_SYSTEM` + `buildContinuousUserPrompt`) plus an output-contract tail — so the brief can't drift from the parser when either is edited. Parse feedback is live (counts, non-blocking notes, and a wrong-mode hint off `detectImportMode`); committing starts a fresh session exactly like a Generate (new sessionId, cleared card states, style stamped) and backfills an empty script box from the storyboard's own lines. Deliberately ungated by script/style — the prompts already exist, and style is only appended at render time. **One-Shot mode is retired** (July 2026): whole script → one multi-cut clip. No model on kie could hold a 30s ad together well enough to ship, so it burned video credits on footage nobody used — revisit when a model can (Seedance 2.5 was the trigger Massimo named). Its view, detail modal and service are deleted (see git history to restore); what stays is the persisted data — `BrollHistoryItem.oneShot*` + `mode: 'oneshot'` in `stores/types.ts`, kept so no row or clip is lost, hidden from History by `isRetiredOneShotRow`. Shared pieces it left behind, still used by the two live modes: all three detail modals share one compact header (`h-12`, `pt-3`) — serif `01` number, vertical rule, one identity pill stacked over the script line; `ModalVideoPlayer` (`cardDetailParts`) gives every modal video tile play-with-sound; `ModelSidePanel` takes `allowedModelIds` (which models are LISTED) + `enabledModelIds` (which are selectable — the rest show greyed/struck-through, same look as `requireMode` dimming) and renders **capability filter pills** (Start + end frame / Start frame / Reference images / Text to video, multi-select) for the video task. **Line-by-Line** (below): script → scenes → **3 variations/scene** (`VARIATIONS_PER_SCENE`), rendered in one row alongside a normal "Add option" card (four equal cells in `xl:grid-cols-4`). It was 4 and the fourth was reliably the dud — three good ideas beat three plus a filler, and every card the member doesn't use is an image (often a video) on their own credits. The parser still READS a `<VAR_4>` (`MAX_PARSED_VARIATIONS`) so older sessions and imports of pre-cut storyboards keep every prompt they were written with. **Delivery toggle** (`BrollDelivery`, left panel): **B-Roll Clips** (default) makes every clip SILENT b-roll — no talking heads, no lip-sync, no spoken line — because a finished voiceover is laid over the footage in the edit; **With Dialogue** turns VAR_1 of every scene into a talking-to-camera `DIALOGUE` card that speaks the line verbatim (`DIALOGUE_DELIVERY_ADDENDUM`, read last so it beats the silent doctrine for VAR_1 only), plus one shared `<VOICE_PROFILE>` appended to every dialogue clip's video prompt at fire time. **Dialogue cards chain like Continuous does**: the ad is one continuous piece to camera cut into pieces, so card N's image gen attaches card N-1's chosen still FIRST under `buildDialogueChainPreamble` — the inverse of `buildReferencePreamble`, keeping the room, wardrobe, light and camera position and changing only the moment (it still asks for the next CUT, never a copy of the reference). `CardState.chainLink` (default on, per-card "Previous cut" slot on the modal's Image tab) switches it off; the chain resolves to the nearest EARLIER dialogue card that has an image, so generating out of order still finds an anchor. Because card N needs card N-1's still to exist, a batch **splits**: b-roll cards fire in parallel and chained dialogue cards run as a queue in scene order, each armed when the previous settles (landed or errored) — see `dialogueQueue`/`dialogueHead` in `ScenesView`. All three variations are **3 genuinely different concepts** for visualizing the line (show-don't-tell: metaphors made literal — "tasted like cardboard" → the character biting actual cardboard — at least one LITERAL variation when the line has a vivid image), LLM-tagged with a lens from a 7-tag menu (Action / Emotional / Product / POV / Environment / Transition / Proof); parser falls back to positional buckets when `<TAG>` is absent. (`ALL_TAGS` is the silent-b-roll menu offered to the model; `PARSEABLE_TAGS` adds `DIALOGUE` — emitted for VAR_1 in With Dialogue — and legacy `STATIC`, whose `buildStaticReferencePreamble` + `startImageTask` `inheritReference` opt survive so old persisted sessions keep rendering.) **Prompt shape:** every prompt is ONE flowing paragraph with **no word limit** — the 40-80-word guide was cut because it capped the model's vision mid-idea; vagueness is the only failure, length isn't — no labelled fields (`PROMPT_FORMAT`, shared by all three prompt sites so they can't drift) — the old six-line `SETTING / CAMERA / ...` structure is gone (it read disjointed and crowded out the idea). Prompts stay silent — no speech, no music — and `withIphoneRealism` appends the realism stack as a trailing sentence (its CAMERA-line branch survives only for legacy structured prompts). `generateNewVariation` / `enhanceVariationPrompt` keep the `<PROMPT>` tag envelope, not JSON. **The camera is a viewpoint, not a prop:** all three prompt sites forbid naming the filming device (phone / front camera / tripod / mirror selfie) and require camera position as geometry — height, distance, angle — because any device noun gets drawn into frame; naming the iPhone as a *quality register* stays fine. PROOF is the one sanctioned exception (a screen as the subject). Every image/video gen gets a deterministic iPhone-realism suffix appended at request time (`services/realism.ts`). Each card has Generate Image **and** Generate Video; video gens are fire-and-forget with refresh-safe resume. Video resolution follows the picked model's `videoConstraints.default` — fresh cards seed from it and the card modal snaps to it on a model flip (Gemini Omni → 1080p, which costs the same as 720p). **Continuous** (`services/generateContinuous.ts` + `ContinuousView.tsx` + `ContinuousDetailModals.tsx`): Zack-D-Films-style **keyframe-chain** ads — one storyboard LLM call turns the script's N lines into N scenes + N+1 keyframes where keyframe N+1 is simultaneously scene N's end state and scene N+1's start, so every clip is a **frames-to-video** gen (first frame = chosen keyframe N, last = keyframe N+1) and the cuts are invisible. Each keyframe fans out as **3 visual concepts** (`CONCEPTS_PER_FRAME`; same story state, different staging — any pick still chains) — three genuinely DIFFERENT ideas for showing the line, each declaring its own **shot size** off `SHOT_LADDER` (`<SHOT>` → `ContinuousConcept.shot`, chip on the card + frame modal header, fed back via `FrameContext.conceptShot` so a rewrite holds its size). There is no scale mandate: fixed Wide/Detail/Character slots were tried and cut — a mandated Wide on an intimate beat leaves nothing worth shooting wide, so the model invents a set. Plus a per-frame Add-concept (drops a blank card you write or Regenerate a prompt in, like Line-by-Line's Add option — no LLM call up front); the user picks one image per frame (auto-picked on a frame's first image; `continuousSelections` keyed by frame index → `{conceptId, imageIndex}`) before any video credits burn. **Chaining a frame to the previous keyframe is ON by default** (`ContinuousFrameCardState.chainLink`, per-frame **Chained/Unchained** pill on the row header beside Generate frame; also a per-concept "Previous frame" slot in the frame modal). It shipped off for a while — all 3 concepts of a frame anchor to one image and come back similar, which weakens the pick-a-concept step — but this is continuous mode, and in practice the continuity is worth more than the spread. Legacy rows that stored an explicit `false` keep it off. The look is held by the storyboard-wide STYLE block plus the character/product refs (pre-style the character in Characters and identity holds far better than a chain image can manage), and the clip is a frames-to-video interpolation that morphs between related frames fine, so long as frame N+1 is a state frame N could plausibly move into. When chained, frame N's gen attaches frame N-1's chosen image FIRST (`buildContinuousPreamble` chain clause, now explicitly forbidding composition/shot-size/angle reuse) plus character/product/extra refs, via `startImageTask`'s `preambleOverride` + `noRealism` opts. **Product visibility is decided per scene** (`<VISIBILITY>` → `ContinuousScene.productVisible`) and refs per concept (`<REFS>` → `ContinuousConcept.refs` → the card's toggles): a beat that attacks the category ("stop eating chalky protein bars") must NOT attach the product reference, or the model renders the member's own packaging as the thing being criticised. Visibility is the hard rule and `parseConcepts` clamps the LLM's refs to it; the prompt must also describe the bad item as an explicit unbranded stand-in ("plain unmarked grey wrapper, no logo"), because omitting the product just lets the model fill the blank from the attached reference. `buildContinuousPreamble`'s `productExcluded` clause says the exclusion out loud so a chained previous keyframe can't smuggle the branding back in. Line-by-Line carries the same doctrine (`clampRefsToVisibility` + rule 2b). **Continuous does NOT segment the script**: `scriptLines` splits it on newlines, the lines are handed to the LLM numbered, and it must return exactly one scene per line with `<LINE>` verbatim — no splitting, no merging, no reordering. Splitting is the user's call, via `SceneEditModal`. (Line-by-Line still carries the older ONE SCENE = ONE IDEA doctrine, which splits a line at a turn like "most taste like cardboard, **but** this one tastes like cookie dough".) **ONE RULE for a scene:** every keyframe is an ACTION that shows what its line SAYS, **caught at the instant that action BEGINS**, and the concepts are three genuinely different ways to do it. The start-frame half of that rule is load-bearing: a keyframe IS the first frame of its clip, so "mid-motion" got read as "draw the action at its peak" and frames came back with the bite already taken — the clip then has nowhere to travel and the model holds still or races past. The prompts say it three ways (`KEYFRAME_FORMAT`, the ONE RULE block, the self-check) plus in `frameBriefBlock` so a single-card Enhance/Regenerate keeps it; the FINAL frame is the one exception — nothing animates out of it, so it may rest. Clarity is the whole point (a scrolling viewer with no sound should be able to guess the narration), which is why **MEDIUM is the default shot** and `SHOT_LADDER` stops at medium-wide: no aerials, no vast establishing shots, nothing that makes the viewer hunt for the subject. A short-lived `<WORLD>` block (one declared location for the whole ad) and a `<TRANSITION>`/anchor system (a connective device plus an element every concept on both sides had to carry) were both tried and **removed** — the world block made every beat the same corner of the same room, and the transition machinery was ceremony around a prompt whose job is "say what moves". What survives of the chain doctrine is one line: frame N+1 must be a state frame N could plausibly move into. Keyframe and motion prompts follow the same **one-flowing-paragraph, no-word-limit + show-don't-tell** doctrine as Line-by-Line (no labelled fields), with keyframes adding the start-frame rule above plus a safe-zone note since 9:16 platform UI overlays the edges. **Safe framing constrains WHERE the subject sits, not how close the camera is** — held in the middle band (centre, or a third at most) with clear margin all round, nothing at an edge or bleeding off, nothing essential in the top/bottom eighth. That rule has now been wrong in both directions: as "centred with comfortable margins" it turned every keyframe into a centred medium, and rewritten to *encourage* off-centre framing it pushed subjects off the 9:16 edges — so it is stated as two separate axes. Camera position is geometry (lens height, distance, angle), the chest-up-portrait-square-to-the-lens shot stays banned, and the ref-copying fix lives in `buildContinuousPreamble`'s character clause: the ref is a chest-up portrait, declared an identity card whose crop must be ignored. **A motion prompt is simple** (`motionFormat()`): what moves and in which direction, how the camera moves, paced to the clip's real seconds, then one sound direction. It was a three-beat scaffold (departure → crossing → settle) with a `<TRANSITION>` device + anchor riding alongside; both were cut as over-built. One line survives because it prevents a real failure: **write the movement, never a picture of the end frame** — painting the final tableau makes the model race there and freeze. **Motion is seeded per-concept, never auto-relinked:** each of a frame's 3 concepts carries its own `motionPrompt`, and picking a keyframe syncs that concept's motion into the clip (a hand-edit sets `motionEdited` and stops the sync). An automatic **two-image vision pass** used to re-run on every keyframe change, rewriting the motion from the two rendered frames — it was removed: an LLM round-trip per pick made choosing keyframes slow and felt like it was fighting the user. The same pass is still one click away as the clip modal's **Regenerate from frames** (`regenerateContinuousMotion`), which is where it belongs — on demand, for the clip that needs it. `ContinuousScene.motionPrompt` survives only as a fallback seed (the first concept's motion) + legacy support. A storyboard-wide `<STYLE>` block rides OUTSIDE the editable prompts (appended at fire time by `buildContinuousPrompt`) so it can't drift per-frame. Style is picked for BOTH modes from one centred popup, `StyleModal` (opened by the left panel's single style row): a **Presets** grid (`CONTINUOUS_STYLES`, whose array order IS the grid order — UGC Realism / 3D Animated / Claymation / Papercraft / Anime / Cinematic; `getContinuousStyle`'s fallback is pinned by id, never `[0]`, so reordering can't silently change what an unknown id resolves to) over a **Your styles** grid (the `styles` bank) and a *New from images* card. **Style is a required input**, shaped like Scripts' Script Style row: the left panel's row is dashed and reads "Visual Style / Select how every clip looks" until a look is actively picked, then fills with the accent tint, the style's name (plus a `Custom` tag) over its brief, and an X to clear — and Generate stays disabled until then (`styleChosen`, persisted; a persisted custom brief counts as picked on its own so pre-flag sessions don't read as empty). "New" leaves the pick alone, exactly as Scripts does. That create view uploads or picks up to 4 frames and runs `analyzeStyleReferences` — a vision pass distilling medium/forms/palette/light/finish into a style paragraph, explicitly forbidden from carrying over the references' subjects. Nothing commits until the user edits/names the result and picks **Use without saving** or **Save to bank & use** (which writes a `StylePreset` row, frames included as `thumbRefs` assets). A custom brief always outranks the preset id; the picked style's `styleBrief` + `styleName` ride the session stamp into `brollHistory` (and `BrollResult.styleName`) so pills read the style's name, not "Custom style". Every style bypasses the app's iPhone-realism stack **except UGC Realism** (`ContinuousResult.realism` → `noRealism: !realism` on both `startImageTask` and `startVideoTask`). The picker LISTS every standard B-Roll video model (`CONTINUOUS_MODEL_IDS`) with `requireMode='frames-to-video'` greying the non-frame models (Kling Turbo, Omni, Grok) so the user sees why they're unavailable; settings key `broll-studio:continuous:video`, default **Seedance 1.5 Pro** (frames-native and materially cheaper per clip than the 2.0 family — a deliberate cost choice, not a regression; it also backs the standalone-Animate default via its `image-to-video` mode); the picker lives **in the clip modal, not the left panel** — the model only matters once there are keyframes to animate. Layout mirrors Line-by-Line: one serif-numbered row per scene — concept cards + Add-concept in a 4-wide grid, with the `Clip N` card pinned in its own right-hand column (thick purple border marks it as the video card) behind a vertical divider, so added concepts wrap below the images and never under the clip — + a closing **Final Frame** row; each row also carries a per-row **Generate frame** button (mirrors Line-by-Line's per-row generate — `requestFrames([idx])`, disabled once that frame's keyframe is picked). **The storyboard is editable in place** (`continuousEdits.ts` — pure ops, applied by `handleEditContinuousStoryboard`): clicking a scene's narration line opens `SceneEditModal` to retype it, **split** it at the cursor (inserting a blank keyframe between the halves), **merge** it with the next scene, or **delete** it. This is also where splitting lives, now that the storyboard never splits a line itself. The frame cards (`${frameIndex}:${conceptId}`), clip cards (`c${sceneIndex}`) and keyframe picks are all keyed by POSITION, so every op declares one old→new index shift and `remapIndexedState` rewrites all three together — never remap one by hand. Affected clips get `motionEdited` cleared so the picked concept's motion re-seeds against the new endpoints. Reordering is deliberately absent: a move invalidates the framing each beat was written to share with its specific neighbours, which is a rewrite, not a move. "Generate frames" renders **every concept of every target frame** in parallel (the batch used to fire `concepts[0]` only, so two of the three cards stayed text and the keyframe was auto-picked before the user saw a choice) — the confirm popup prices images, not frames, and the pick is deterministic (the previously-picked concept, else the leftmost that landed) rather than whichever generation finished first. The per-row button stays live once a frame is picked (it re-arms `includeExisting` and reads **Regenerate frame**) — disabling it was a dead end out of the auto-pick. Falls back to a sequential index-order walk only when some frame in the batch is Chained and has to wait on the one before it, "Generate all clips" fires ready clips in parallel behind a cost-confirm popup (clip count + estimated credits + balance), and **Download clips** (shown once any clip exists) opens `ClipDownloadModal` — a tick-list of every rendered clip, pre-ticked to each card's COVER take, zipped via `utils/downloadZip.ts` (jszip). Both modes share it. It replaced a "Download all" that swept every take of every card into the zip, so a card regenerated three times landed three clips. **Cover take** = what the card face plays and what its hover Download saves: Line-by-Line sets it from the card modal's gallery (`CardState.selected`), Continuous from the clip modal's take gallery (`currentVideoIndex`) — both badge the picked tile "Cover". Clip cards + gallery tiles keep their hover **Download**. Frame cards open `ContinuousFrameModal` — full Line-by-Line image parity: `ModelPicker`, chain/character/product `ReferenceSlotCard`s + `ExtraRefsRow`, Enhance / Regenerate / Undo / Redo (`enhanceContinuousFrame` / `regenerateContinuousFrame` + `frameContextFor`, which feeds the LLM the motions either side so a rewrite still chains), resolution/aspect chips, credits pill, and an image gallery where clicking a tile makes it the keyframe. Clip cards open `ContinuousClipModal` (model picker, endpoint thumbs, motion prompt with **Enhance / Regenerate from frames / Undo / Redo** — Regenerate does the two-image vision pass via `regenerateContinuousMotion`, falling back to start-frame-only until the end keyframe is picked — res/duration/audio, credits chip). Clips inherit their **start keyframe's aspect ratio** (clamped to the video model's list), not a hardcoded 9:16, so 16:9 storyboards animate on the canvas they were drawn for. Sessions ride brollHistory (`continuous*` optional fields) + the same videoHistory/resume machinery; no-key click shows `buildDemoContinuousResult`. |
| Create | Ad Analyzer | `ad-anatomy/` | Ad image or video frame → Breakdown (scorecard + hook / angle / structure + a product-agnostic Script Style Prompt, saveable to the Script Bank as `kind: 'style'`) + transcript + visual playbook. Runs as TWO chat passes at high reasoning (`analyzeAd.ts`): perception (video + client-side cut-point keyframes from `utils/extractKeyframes.ts` → transcript + per-cut shot log + character/product/setting dossiers) then text-only synthesis (shot log → scorecard/breakdown/scene prompts). Pass-1 output persists on the history row as `perception`, so pass 2 resumes/restarts after refresh without the file; legacy single-pass taskIds are detected and completed as-is. |
| Tools | Playground | `playground/` | Free-form Image / Video / Music. Prompt bar, mode tabs, model picker, model-aware ref slots + drag-drop (images everywhere; Seedance 2 adds audio/video reference clips; Gemini Omni adds characters / designed voices / a trimmed source clip under a 7-slot quota). `@`-mentions reference Products / Influencers / B-Rolls and auto-attach the asset. Uploaded audio/video refs are memory-only (pruned from the persisted draft — too big for localStorage). |
| Create | Edit | `edit-studio/` | Download + setup page for the `/video-editor` Claude skill (script + voiceover + B-roll in → finished captioned 9:16 ad out, edited locally by Claude Code — no kie.ai involvement). Hero is a hover-animated ivory folder (`SkillFolder.tsx`, orange glow, work-cards rise out on hover) that downloads `public/video-editor.skill` (keep that copy in sync with the source skill); beside it, a 4-step setup card in the ApiKeyGuide style. Slug `/edit`; crab: Snips · Editor (clapperboard). |
| System | Dashboard | `dashboard/` | Default landing page. Greeting (profile first name, Instrument Serif italic like all hero stats) + bento value metrics: time saved (per-kind minutes + a task-switching constant, both in `utils/usage.ts`), money saved vs the higher of official-API and creator-platform pricing — both heroes carry a green rolling-7-day "+N this week" delta — current/longest streak, active days, 26-week activity heatmap (green ramp), Get Credits/Community quick links, and crab-crew shortcut tiles (dock order, `ArrowUpRight` → `openApp`). Until a kie key is saved, a slim red to-do row (→ the menu bar's ApiKeyGuide steps → Settings) sits above the metrics. Everything derives from the `usageDays` ledger (see Banks) — the app never writes data. The menu bar shows a flame "N day streak" chip (opens Dashboard) whenever the streak is live. |

## Role

Senior frontend engineer + product architect. Push back when something's flawed. Ask for context (kie doc URLs, exact error text) before guessing. Prefer the boring, debuggable solution.

## Core Rules

**Code philosophy.** Simple > clever. Obvious > terse. Components small and single-purpose. No premature optimisation. Comments explain *why*, not *what*.

**Architecture.**
- **Self-contained apps.** Each app under `src/apps/<name>/` owns its types, components, and service. Cross-app communication goes through the bank store (persistent) or the inter-app payload (one-shot handoffs).
- **No prop drilling.** Zustand stores for global state; local React state for ephemeral UI.
- **Model registry is the single source of truth.** Add or change a model only by editing `src/utils/models.ts`. Don't sprinkle slugs through service files.

**Styling.** Tailwind only. Per-app accent palettes are custom Tailwind families (`influencers-*`, `scripts-*`, `voice-*`, `broll-*`, `playground-*`) defined in the `@theme` block of `index.css` (Ad Analyzer uses literal `#FF5257`); the matching hexes for sidebar/bank chrome live in `constants.ts`. Change an accent there, not by sprinkling new color classes. Tracking-tight text. Transitions 200–300 ms for panels, 150 ms for hover. Tags use `TAG_STYLES` from `models.ts` (Recommended green / New fuchsia / Fast sky / Cheap zinc). **Single-line inputs, select-style triggers, chips, and buttons are fully rounded (`rounded-full`), not rectangles** — Massimo's preferred aesthetic; multi-line textareas use `rounded-2xl`.

**Shared UI idioms.** Reach for these instead of re-implementing — each one had drifted into 3–4 near-identical copies before it was extracted:
- `components/tileActions.tsx` — the hover action column on every generated media tile. `TileActionStack` + `TileActionButton` + `TileStarButton` + `TileDeleteButton`, in the order **[Star] → Download → Save → Copy → extras → Delete**. 32px circles, `bg-black/55`, no backdrop-blur (it stutters under the opacity fade).
- `TileDeleteButton` is the **only** delete idiom: two-click, arms into a red "Confirm" pill, reverts after 3s. Never a confirm modal. `variant='chrome'` for panel surfaces, `size='sm'` for compact history rows.
- `components/GeneratingMedia.tsx` — the one "this is generating" face. `GeneratingMediaFill` drops into any `relative overflow-hidden` frame (a card face); `PendingMedia` is the standalone aspect-sized tile for gallery grids. A card mid-generation and that same generation inside its detail modal must look identical, so both render this: blob backdrop, mode glyph top-left, model name, rotating status line (same size + accent tint as the model name — never a dim grey), prompt fading out along the bottom. Status-line copy lives in `components/generatingMessages.ts`.
- `components/GeneratingChip.tsx` — `GeneratingChip` (pulsing accent dot + label) and `GeneratingPulseRing` (thumbnail overlay) mark a **history row** whose work is still running. **History is the queue view**: fire a batch, switch away, and the list keeps reporting progress. Wired in the Ad Analyzer rail (its own literal-hex copy), B-Roll History (counts tallied from each row's snapshotted in-flight queues, TTL-swept at 30 min), Voiceovers History (an "In progress" section above the finished rows), and Playground / Influencers galleries (their own in-flight tiles).
- **Generate buttons never disable while a generation is in flight.** Every media surface queues gens in parallel; the in-flight tile is the feedback, not a locked button or a spinner swapped into the label. Guard duplicate fires of the *same* card with a `startingRef`-style set, not by disabling. The only legitimate blocks are structural (Continuous' sequential keyframe chain, nothing to generate yet).
- `components/DayPill.tsx` — the day separator on **every** history surface (Characters, Playground, Scripts, B-Roll, the B-Roll card-detail galleries): a small centred pill reading "Today" / "Yesterday" / a full date (`sectionLabel`), or an in-flight label like "In progress". Never a left-aligned heading with a hairline rule — that shape was tried on the two card grids and read as a divider between unrelated blocks. `className` sets the vertical margin only (pass `""` when the list already spaces its groups).
- `hooks/useCloseOnEscape.ts` — every dismissible overlay takes it, alongside the existing backdrop click (and `useCloseOnAppSwitch` when body-portaled).
- `hooks/useInlineVideo.ts` — **one clip plays at a time, app-wide.** Every video tile that can make noise (B-Roll's variation + clip cards, the detail-modal galleries, Playground's history grid) uses `useInlineVideo`: spread `hoverProps` on the tile root (never the `<video>` — the overlay buttons aren't its DOM children, so hovering one would read as a mouseleave) and `videoProps` on the element. Hover autoplays muted; the Play button is a user gesture, so it plays with sound, claims the single module-level playback slot, and pauses whoever held it (across apps — background apps stay mounted). A silent hover preview stands down while another clip holds the slot, and Play only *pauses* what it started, so clicking it during a preview means "let me hear it". `watching` (playing **and** unmuted) is the "user is watching this" flag: pass it to `TileActionStack hidden={…}` and hide the tile's other hover affordances, leaving only play/pause + mute on the media. Players that keep native `controls` (the Playground lightbox, the Ad Analyzer source clip) take `useExclusiveVideo` for the same one-at-a-time rule and nothing else.
- `components/ClearAllButton.tsx` — the panel-level reset at the top of each input column. Labelled **"New"**, because it clears inputs only; outputs and history always survive.
- Panel headers are `h-[57px]` + `border-b border-ink/5` on **both** panes of a two-pane app, rendered even when a pane is empty, so the hairlines align.
- No global focus ring, on purpose. A `:focus-visible` outline was tried and reverted: `:focus-visible` matches text inputs on a plain mouse click too, so every search field and textarea drew an outline around its own border. The `focus:outline-none` resets stand — see the note in `index.css`.

**Theming (light default + dark mode).** Driven by `data-theme` on `<html>`: an inline script in `index.html` sets it pre-paint; `stores/themeStore.ts` owns the preference (`dark`/`light`/`system`, own localStorage key `ai-ugc-lab-theme`, deliberately per-browser — never cloud-synced, survives sign-out). New browsers default to `dark` — the store fallback and the index.html bootstrap must agree. Switchers: Settings → Appearance + the menu bar's top-right quick toggle. UI chrome must use the semantic tokens from `index.css`, never literal white/zinc/dark hexes:
- `ink` — fg chrome (white in dark, near-black in light): `border-ink/10`, `bg-ink/5`, `text-ink`. `paper` is its inverse (`bg-ink text-paper` buttons).
- `surface-0/1/2` — page → panel → popover elevation.
- `ink-50…950` — text ramp; dark equals zinc, light is mirrored (ink-100 stays "bright", ink-500 mid, ink-700 dim). Inverse elements self-flip (`bg-ink-100 text-ink-900`).
- Accent families auto-flip their 100–400/600 tints in light mode.
Exceptions that stay literal: anything overlaying user media (badges/scrims/play buttons → `text-white`, `bg-black/60`, `from-black/85`), white text on solid accent buttons, modal backdrops. Status tints (-100…-400 colored text on tinted bg) need a `light:` darker variant (`text-red-300 light:text-red-700`; the `light:` custom variant is defined in `index.css`). `AppBackground.tsx` holds the per-theme canvas gradient.

**Errors.** End-user generation surfaces show friendly copy via `humanizeError()` (see `utils/friendlyError.ts`). It always returns a **complete sentence** — toast it verbatim and name the operation in the `fallback`; never prefix the result (``addToast(`Video generation failed: ${msg}`)`` printed the operation twice). Infra/admin/Settings surfaces still surface the raw kie.ai message — the operator debugs there. Note: the kie envelope can return `{ code: 5xx, msg: "...maintained..." }` inside an HTTP 200 during maintenance windows — not a code bug.

## Tech Stack

- React 19 + TypeScript + Vite (`npm run dev` → http://localhost:5173). Tailwind CSS 4. Zustand for global state.
- IndexedDB (`assetStore.ts`) for blobs, mirrored to Cloudflare R2 when cloud sync is active. localStorage for bank metadata, settings, picker selections.
- **Cloud (opt-in):** Supabase (auth + Postgres) + R2. Enabled when `VITE_SUPABASE_URL` + `VITE_SUPABASE_ANON_KEY` are set; absent → local-only mode (100% client-side). Access gated by email allowlist synced from Skool via Zapier.
- kie.ai Bearer token sent directly from the client (no proxy).

## kie.ai client (`src/utils/kie.ts`)

Four transports — pick by task type. Endpoints and result shapes live in `kie.ts`; the gotchas worth knowing here:

1. **Async task** (`createTask` → `pollTask` → `parseResult`) — `POST /jobs/createTask` → poll `GET /jobs/recordInfo`. Result URLs are string-encoded JSON inside `resultJson.resultUrls`. Outputs expire in 3 days — always `downloadAsBase64` + `saveAsset`. The shared tails `finishImageAssetTask` (`utils/imageTask.ts`) and `finishVideoAssetTask` (`utils/videoTask.ts`) wrap poll→download→save for every image/video surface; the video one adds the black-tile guards (0-byte / non-video content-type / `probeVideoBlob` decode check).
2. **Chat completions** (`kieChatCompletions`) — `POST /<slug>/v1/chat/completions`, streaming SSE, falls back to JSON parse then surfaces the body's first 400 chars on failure. Vision via `image_url` blocks (data URIs work, no upload). Get the endpoint path from `getChatEndpointPath()` — never hardcode slugs.
3. **Veo** (`kieVeoCreate` → `kieVeoPoll`) — custom `/veo/generate` + `/veo/record-info`. Variant (`veo3` / `veo3_fast` / `veo3_lite`) via the `model` field, not the URL. Results nest under `response.resultUrls`.
4. **Suno music** (`kieMusicGenerate` → `pollMusicTask`) — custom `/generate` + `/generate/record-info`. Suno-specific status values; `callBackUrl` is required by the schema but we pass a no-op and poll. Audio at `response.sunoData[].audioUrl` (v1 uses the first track).
5. **Omni create** (`kieOmniAudioCreate` / `kieOmniCharacterCreate`) — synchronous `POST /omni/audio/create` and `/omni/character/create`, no polling. Mint persistent `kieAudioId` / `characterId` values (scoped to the member's kie account) that `gemini-omni-video` consumes as `audio_ids` / `character_ids`. Success can come back as envelope code 0 *or* 200.

`ensureHostedUrl(apiKey, source)` uploads `data:` URIs to kie's file host (image→image and frame-conditioned video models need public URLs; hosted files expire in 3 days). `kieTestConnection` checks the credit balance (Settings).

## Models (defaults)

Full list in `src/utils/models.ts`. Defaults below; users can swap image and video models from the picker.

| Capability | Default | Notes |
|---|---|---|
| Text + vision | Gemini 3 Flash (`CHAT_MODEL_DEFAULT`) | Hard-coded **everywhere** — no picker, and as of now no exceptions. The last two holdouts came back within days of each other: **Scripts** (`generateScript.ts`, generation + `enhanceBrief`), on the theory that prose a human reads earns the upgrade, and **product auto-fill** (`finder/extractProductInfo.ts`), on the theory that a misread spec propagates into every ad written off that product. Both moved back for the same reason the app-wide swap did: ~2.6× the credits for output that didn't visibly improve, and members pay it on their own key. Auto-fill also gained extra product angles, which give the read more to go on than a stronger model did. `getChatEndpointPath`'s default; includes the Ad Analyzer's two video passes (createTask jobs API, streaming fallback pinned to the same model). Registry order matters — this entry is **first** so it stays `getDefaultModel`'s `candidates[0]` fallback. |
| Text + vision (strong tier) | Gemini 3.6 Flash (`CHAT_MODEL_STRONG`) | OpenAI-compat slug `gemini-3-6-flash-openai`. **Currently zero call sites** — the constant stays exported as the named opt-in for a surface where a wrong answer would cost real rework. **Services name a role, never a slug:** both constants live in `models.ts`, so moving a surface between tiers is a one-line edit there. Don't delete it to chase an unused export; do check `git log` before promoting anything back, since both prior tenants were measured and returned. |
| Image (text→image) | Nano Banana 2 (`nano-banana-2`) | App-wide default (leads the registry); Influencers defaults to GPT Image 2 (`gpt-image-2-text-to-image`). Picker also: GPT Image 2, Seedream 5.0 Pro (`seedream/5-pro-text-to-image`) |
| Image (image→image) | Nano Banana 2 (`nano-banana-2`) | Used when reference images are present. Picker also: GPT Image 2 Edit, Seedream 5.0 Pro Edit (`seedream/5-pro-image-to-image`, hidden auto-swap sibling of Seedream 5.0 Pro) |
| Video | Gemini Omni (`gemini-omni-video`) in both B-Roll and Playground | Ref-capable, but **no `image-to-video` mode** — it takes images as generic references, not as frame one, so B-Roll's Animate tab greys it out and `runVideoTask` refuses rather than silently dropping the still. Picker also: Seedance 2.0 (`bytedance/seedance-2`), Seedance 2.0 Fast, Seedance 2.0 Mini (`bytedance/seedance-2-mini`, ref-capable, 480p/720p only), Seedance 1.5 Pro (`bytedance/seedance-1.5-pro`, `input_urls` frames, no reference inputs), Veo 3.1 Fast/Lite/Quality, Kling 3.0, Kling 3.0 Turbo (`kling/v3-turbo-image-to-video`, image-to-video only), Kling 3.0 / 2.6 Motion Control (`kling-3.0/motion-control`, `kling-2.6/motion-control` — Playground only), Wan 2.7 |
| TTS | Gemini 3.1 Flash TTS (`google/gemini-3-1-flash-tts`) | Hard-coded; 30-voice catalog, slide-in picker. Token-metered pricing (see `geminiTtsCredits`) |
| Music | Suno V5 (`suno-v5`) | Playground only. Picker also: Suno V5.5 |

- **Pricing.** `estimateCredits(modelId, params)` + `formatCredits` are the only public credit APIs. Each `ModelEntry` declares flat `pricing.credits` or a `priceFor(opts)` callback for multi-dimensional models (Kling, Veo). No USD is shown in pickers/generate buttons (credits only). Per-call models (Veo all variants) have `videoConstraints.durations: []` so the duration toggle hides.
- **Official pricing / savings.** `ModelEntry.official` = `{ usdFor(opts), source }` — the provider's own API list price — and `ModelEntry.market` — a verified creator-platform price (Higgsfield etc.). Both only added when verified (source URL in the entry; return `null` for tiers with no comparable rate — unknown savings count as zero, never invented). `CREDITS_PER_USD = 200` ($0.005/credit). `officialSavingsPercent(modelId)` (official-only) drives the green "% off" chip in ModelPicker + ModelSidePanel; the Dashboard's money-saved metric compares kie against `max(official, market)` per generation (USD appears there deliberately). Seedance 2.0 family has NO official entry on purpose — kie beats Fal but not BytePlus direct, so we claim nothing (see registry comment).
- **Video modes** (`text-to-video` / `image-to-video` / `frames-to-video` / `reference-to-video` / `motion-control`). Each model declares `videoModes` + `videoConstraints`. B-Roll and Playground infer the mode at generate time from which inputs are filled (references → reference-to-video; start+end → frames; start only → image; none → text). Ref-capable models: `veo3_fast`, `bytedance/seedance-2`, `bytedance/seedance-2-fast`, `gemini-omni-video`.
- **Motion Control** (`kling-3.0/motion-control`, `kling-2.6/motion-control`, Playground only). Flagged `motionControl: true`; mode is fixed (not inferred). Takes a required character image + driving video (`input_urls` / `video_urls`) and a `character_orientation` ('video' ≤30s / 'image' ≤10s); prompt optional, no aspect/duration/audio. Playground swaps the ref panel for `MotionControlSection` (image slot + driving-clip upload + orientation toggle) via the `motion-image` / `motion-video` ref slots; the driving clip is memory-only (pruned from the persisted draft). Per-second billing keyed on 720p/1080p.
- **Gemini Omni** (`gemini-omni-video`). The video default everywhere; its *extra inputs* (characters / designed voices / source clip) are Playground-only UI — B-Roll drives it with plain reference images. Three capability flags on `ModelEntry`: `supportsReferenceAudio` / `supportsReferenceVideos` (Seedance 2 family — `reference_audio_urls` / `reference_video_urls`, ≤15s total each, validated client-side via `utils/media.ts` metadata reads) and `omniInputs` (Omni only). Omni inputs share a 7-slot quota (image ×1, source clip ×2, character ×1). Characters come from the Influencers bank — `ensureOmniCharacterId` (playground/service.ts) mints the id via `/omni/character/create` on first attach and stamps `Model.omniCharacterId`. Designed voices live browser-locally in `stores/omniVoiceStore.ts` (like the kie key — ids are kie-account-scoped). Omni audio is always on (no toggle); a video input switches billing to a flat per-call tier (`PriceParams.videoInput`).
- **Body shaping.** `buildImageInput` / `buildVideoInput` / `buildMusicInput` produce the correct per-model request body from a uniform options object (handling field-name differences like Seedance `first_frame_url` vs Veo `imageUrls`).
- **B-Roll per-card video.** Each card carries its own `videoStatus` + persisted `videoTaskId`, so cards generate in parallel and survive a refresh. `services/generateVideo.ts` splits into `startVideoTask` (→ taskId) and `finishVideoTask` (poll → download → save). OutputPanel resumes pending tasks on mount; entries older than 30 min are evicted with a Reset link.

## Non-obvious files

Most files are self-explanatory. These carry behaviour worth knowing before you touch them:

- `stores/bankStore.ts` — all banks + history + `migrateVoiceShape`.
- `utils/orphanCleanup.ts` — once-per-sign-in asset sweep. Its bank list **must** cover every bank (compile-time `satisfies Record<BankKey, true>` guard) or it deletes that bank's live assets from IDB + R2. Asset refs come in two shapes (bare `asset-x` and `asset://asset-x`); anything comparing refs against `assets.id` must normalise via `assetIdFromRef` first — unnormalised comparison once purged every B-Roll video.
- `utils/assetStore.ts` — IndexedDB blobs + fire-and-forget R2 mirror; `getBlob()` falls back to R2 on miss.
- `utils/friendlyError.ts` — `humanizeError(err, fallback)`, the ordered rule table for end-user error copy.
- `hooks/useCloseOnAppSwitch.ts` — every body-portaled overlay (slide-over, picker, modal) must call it: portals escape the opacity-0 wrapper that hides background apps, so an open overlay would float over the next app after a dock switch.
- `lib/cloudSync.ts` — pull on sign-in + debounced diff-push + persistent localStorage outbox + non-destructive hydrate. `saveRow`/`deleteRow` are serialised per row (a stalled upsert can't land after a delete) and outbox markers are token-guarded (a late success can't clear a newer pending write). `walkAssetRefs` normalises `asset://` refs to bare ids so the sweep/reconcile match the bare-keyed `assets` table.
- `lib/supabase.ts` — `ensureFreshSession()` (3s race + cached-token fallback) and a custom non-blocking `auth.lock`.
- `hooks/useAssetUrl.ts` — resolves `asset://` refs to blob URLs.
- `UI_LAYOUT.md` (repo root) — spatial map of the rendered interface (pane splits, tab orders, button positions, modals) for every screen. Read it before any UI-driven / tutorial automation so you don't guess where a control sits.

## Banks

Persisted to `localStorage` under `ai-ugc-lab-banks`. Asset blobs live in IndexedDB (mirrored to R2 in cloud mode); rows store `asset://<id>` refs. `useAssetUrl(ref)` turns a ref into a blob URL. Types are in `stores/types.ts`.

`products`, `models` (from Influencers), `scripts`, `voices`, `brolls`, `styles`, plus auto-pushed history banks: `voiceHistory`, `videoHistory`, `imageHistory`, `musicHistory`, `scriptHistory`, `brollHistory`, `characterHistory`, `adAnatomyHistory`, and the Dashboard's `usageDays` ledger (all cloud-synced — every bank table has a Postgres mirror). Gotchas:

- **`usageDays`** — accumulate-only usage ledger: one row per local calendar day (`id: 'YYYY-MM-DD'`), holding per-kind generation counts + estimated kie credits + official-API USD. Written by `bankStore.recordUsage`, which is called inside the `add*History` actions (plus B-Roll's `finishImageTask` and the Ad Analyzer complete-flip) — new generation surfaces get counted for free if they push history. Deleting/clearing history never subtracts. One-time backfill from history (`backfillUsageLedger`, flag `ugc-lab:usage-backfill:<uid>`) runs post-hydrate in cloudSync (local-only: Dashboard mount) and skips ids in the mock-data manifest. Mock seeding suppresses live recording via `setUsageRecordingSuppressed`. Its Postgres table `usage_days` (migration 0016) uses a **composite PK (user_id, id)** because day keys repeat across users — run the migration before deploying, or every hydrate reports a per-table error (which also skips the auto orphan sweep).

- **Starring.** `products` / `models` / `scripts` / `brolls` / `styles` items carry `starred?: boolean` — flipped by bankStore's silent `toggleStar` (no toast) from a star button in the Bank cards' hover stack. Starred items sort first in every `BankPicker` (`starredFirst` in `finder/bankSort.ts`, amber badge on the card); the Bank browser's own sort order is deliberately untouched (B-Rolls are day-grouped).

- **`styles`** — visual looks for B-Roll (name + `brief` + up to four `thumbRefs`). The brief IS the style: it's the paragraph appended to every prompt rendered in that look, so it must describe the LOOK only, never anything from the frames it was read from. Thumbnails are this bank's own assets (nothing else links them), so delete/replace purges them outright. Postgres table `styles` (migration 0017) — run it before deploying, or every hydrate reports a per-table error (which also skips the auto orphan sweep).
- **`brolls`** — only **stills** are saveable to the bank (reusable as start frames / references); videos are download-only, so no UI writes new `videos[]` entries anymore (the save-to-bank action is hidden on video tiles in B-Roll + Playground). The `videos[]` field and its render/edit/cleanup paths stay for back-compat — older saved-video entries (and the video-only-broll render path in `BankList`) still display and download. Saved video-history items stamp `linkedBRollId`; deletion only purges the blob if not linked.
- **`videoHistory`** is shared by B-Roll and Playground (for refresh-resume); Playground's grid filters out `sourceApp === 'broll-studio'`. No automatic retention — entries persist until the user deletes/clears them. Only `brollHistory` is capped (`BROLL_HISTORY_CAP`, FIFO); the other history banks (`videoHistory`/`imageHistory`/`musicHistory`/…) grow unbounded, so an eventual cap is the most likely localStorage-quota mitigation if it's ever needed.
- **`characterHistory`** carries `imageRef` + the full `profile` snapshot; `linkedModelId` is set on save-to-bank and gates blob purge. Presets aren't a bank — reuse a recipe by saving to `models` and reloading via the Controls header dropdown.
- **`VoicePreset` / `VoiceHistoryItem`** carry the Gemini TTS param set (`voiceId` = `voice_name`, `style`, `pace`, `accent`, `temperature`, optional `scene` / `sampleContext`). `migrateVoiceShape` strips the legacy ElevenLabs numeric fields (`stability` / `similarityBoost` / `speed`, and the old numeric `style`) and backfills the new string defaults (`Natural` / `Natural` / `Neutral`, temp `1`).

## Auth + cloud sync

`AuthGate` wraps the app: bootstrapping → spinner; signed-out → `AuthScreen`; signed-in → workspace + `UserMenu`. No cloud env vars → local-only mode with a bottom banner. Full deploy stack is in `DEPLOYMENT.md`; threat model in `SECURITY.md`.

- **Allowlist.** Postgres trigger `enforce_allowlist` blocks signups not in `public.allowlist` (no client bypass). A global `public.app_config.enforce_allowlist` flag (Admin → Allowlist toggle) short-circuits the trigger when off — open signups while Zapier isn't wired; flip back on to re-gate. `on_allowlist_delete` sets `profiles.disabled_at` → app signs the user out on hydration; `on_allowlist_insert` clears it. A disabled account (sign-in or stale session) sets `authStore.accessRevoked`, and `AuthScreen` shows a "members only" popup linking to `SKOOL_COMMUNITY_URL`. Zapier syncs Skool events in. **Names:** signup collects First name + Surname → passed as auth user metadata; `on_auth_user_created` seeds `profiles.first_name/last_name` from the allowlist row, falling back to that metadata (migration 0015) so members who sign up with a non-Skool email are still identifiable.
- **Cloud sync** (`cloudSync.ts`). Pulls profile + all bank tables on sign-in. `bankStore` writes local (Zustand + localStorage) first, then fires the cloud push in the **background** — `pushRow`/`dropRow` are fire-and-forget (return `void`, never awaited). The UI must never `await` a cloud round-trip: awaiting it was the "save stuck loading, then times out after ~15s" hang, and it could drop the row on a mid-hang refresh. Durability comes from the outbox, not the await — `recordPendingUpsert`/`recordPendingDelete` persist the row to a per-user localStorage outbox (`ugc-lab:sync-outbox:<uid>`) **synchronously** before the push fires, so a failed/timed-out push is replayed by `drainOutbox()` (startup + tab focus + backoff timer) and overlaid onto the next hydrate. Background push failures are logged, not toasted — local is the source of truth, sync is eventual. **Hydrate is non-destructive:** a per-table fetch error keeps local rows (never `[]`), and outbox-pending rows are overlaid so an unsynced row survives a refresh.
- **Session freshness** (`supabase.ts`). Every cloud write awaits `ensureFreshSession()`, which races `getSession()` against a 3s timeout and falls back to a module-cached token (kept current by an `onAuthStateChange` listener) when the SDK's auth lock stalls after a backgrounded tab returns. The client also uses a custom `auth.lock` (`nonBlockingLock`, ~2s bound then runs unlocked) because supabase-js takes that lock on every request. `visibilitychange` recovers the token on focus.
- **kie.ai key is browser-local only.** Lives in `localStorage` under `ai-ugc-lab-settings`, never written to or read by Supabase/cloudSync. Users re-paste on each new browser. Deliberate trade-off: nothing in the DB ever holds the key.
- **Schema.** Bank tables are JSONB (id PK, user_id FK, project_ids[] GIN-indexed, data jsonb). RLS `auth.uid() = user_id` everywhere, admin bypass via `profiles.is_admin`. Admin app (Members + Insights + Allowlist) only renders for admins; the Members table and the Insights charts share one fetch via `apps/admin/useMembers.ts`.

## Inter-app payloads

One-shot handoffs. Sender calls `sendToApp({ targetApp, targetField, data })`. Consumer reads `interAppPayload` in a `useEffect` keyed on `activeApp`, dispatches on `targetField`, then calls `consumePayload()`. Wired: Ad Analyzer → Scripts (transcript) / Bank (productId); Scripts → Voiceovers (script text); Scripts → Playground (`cinematicVideo`: a `CinematicVideoPayload` of resolved prompt + product/influencer refs + modelId + duration → opens video mode, refs attached, Seedance default); B-Roll Bank → Playground (`videoStartFrame` opens video mode with the still as start frame + prompt prefilled); B-Roll video → Playground (`videoSourceClip`: a `VideoSourceClipPayload` asset ref + real clip duration → video mode switched to Gemini Omni with the clip attached as the trimmed `omni-clip` source clip — the "same take, dialogue in Spanish" redub loop; the asset ref survives refresh, unlike uploaded clips); Anywhere → Playground (`prompt`, `imageRef`, `videoStartFrame`).

## Recent changes

This file no longer keeps a changelog — read `git log` for history. Most recent coherent work: friendly, consistent end-user error copy via `utils/friendlyError.ts`, wired into every generation catch across the apps.

## Keeping the local folder synced

Massimo's primary Mac checkout lives at `/Users/massimoenzo/Documents/AI Shortcuts/UGC OS` and must always reflect the latest **merged** code on `main`. **After you merge a PR to `main`**, update that folder so it tracks the newest merged PR:

```
cd "/Users/massimoenzo/Documents/AI Shortcuts/UGC OS" && git checkout main && git pull origin main --ff-only
```

Only do this when its working tree is clean (`git status --porcelain` empty) — never discard uncommitted work. Worktrees under `.claude/worktrees/` are separate checkouts and are unaffected.

## When making changes

After non-trivial changes to behaviour, file structure, or the model lineup:

1. Keep this file accurate — it's both your guardrail and per-session context, so keep it **lean**. Don't add a changelog; that's what `git log` is for.
2. Update the model table when you register or remove a model.
